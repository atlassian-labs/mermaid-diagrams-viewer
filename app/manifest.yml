app:
  id: ari:cloud:ecosystem::app/6ab5bc71-c8ef-4e78-9e33-5c250f01b073
  runtime:
    name: nodejs24.x

modules:
  macro:
    - key: mermaid-diagram
      resource: custom-ui
      title: Mermaid diagram
      icon: resource:icons;app-icon.png
      description: Render a Mermaid diagram from a code block on a page
      categories:
        - formatting
        - confluence-content
        - visuals
        - development
      config:
        resource: custom-ui
        viewportSize: small
        title: Mermaid diagram configuration
      resolver:
        function: resolver

  function:
    - key: resolver
      handler: index.handler

  objectStore:
    - key: icon-packs
      storageClass: standard

resources:
  - key: custom-ui
    path: build/custom-ui
    tunnel:
      port: 5173
  - key: icons
    path: icons

permissions:
  scopes:
    - read:page:confluence
    - storage:app
  content:
    styles:
      - unsafe-inline
